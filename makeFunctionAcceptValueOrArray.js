

function refresh( wrapperNameArray ) {
    // if a single string specified, convert it to array
    if( typeof wrapperNameArray === 'string' ) {
        wrapperNameArray = [ wrapperNameArray ];
    }
    
    ...
}
